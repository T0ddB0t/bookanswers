class one{
private:
    int no = 1; //can only be used in in class one
protected: 
    char mby = '-'; //can only be used in other classes
public:
    bool yes = true; //is a slut and is used everywhere
};
